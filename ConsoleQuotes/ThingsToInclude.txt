Things and equations to Include:

If order filled then Pip.value in cell. Want toknow and keep track of the spread when an order is placed

if AUD/USD spread is > 1.3 or avg over 3 min do not buy/short order

writing to csv skip the 1st 7 line or delete the 1st 7

Figure out the volume issue being called wrong. or If vol == vol[1] && lastVol[1] == last vol then Volume and last volume = 0; 

			//  Snippits
            // client.Add(Symbols.Aud, QuoteType.Open); // Call once at the begining of the day
            // client.Add(Symbols.Aud, QuoteType.High); // Call if HH > last HH
            // client.Add(Symbols.Aud, QuoteType.Low); // Call if LL is < Last LL
            // client.Add(Symbols.Aud, QuoteType.Close); // Call once at the close and opeing of the day
            // client.Add(Symbols.Aud, QuoteType.PercentChange); call periodilcy to xheck if prices are in sync
            // client.Add(Symbols.Aud, QuoteType.BID_SIZE);


			get delta time

			add share count and p/l day